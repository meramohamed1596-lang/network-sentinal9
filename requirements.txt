Flask==2.3.3
Werkzeug==3.0.1
streamlit>=1.26.0
requests>=2.31.0
# Professional network automation libraries
netmiko>=4.3.0          # SSH/CLI management for Cisco, Huawei, Juniper, and 50+ vendors
napalm>=5.0.0           # Multi-vendor network automation (get/set configs, show commands)
ntc-templates>=4.0.0    # TextFSM templates to parse show command output into structured data
textfsm>=1.1.3          # Parser for converting CLI output to structured data
ciscoconfparse>=1.9.0   # Parse and query Cisco IOS-style configurations
netutils>=1.6.0         # Network utility functions (IP, MAC, VLAN, etc.)
